{duration,2}.
{concurrent,25}.

% uniform_barrier
% readonly_blue
% writeonly_blue
% read_write_blue
% readonly_blue_barrier
% writeonly_blue_barrier
% read_write_blue_barrier
% readonly_red
% writeonly_red
% read_write_red
% readonly_red_barrier
% writeonly_red_barrier
% read_write_red_barrier
{operations, [
    %% 70% blue, ~15% updates
    {readonly_blue, 60},
    {writeonly_blue, 10},
    %% 30% red, ~15% updates
    {readonly_red, 26},
    {writeonly_red, 4}
]}.

{readonly_ops, 3}. %% R
{writeonly_ops, 3}. %% W
{mixed_read_write, {1, 1}}. %% R, W

% grb_lww | grb_gset | grb_gcounter | grb_maxtuple
{crdt_type, grb_lww}.

{reuse_cvc, true}. %% Should enable for updates
{retry_aborts, false}. %% Only for red transactions

% {key_generator, {uniform_int, 1000000}}.
{key_generator, {function, hook_grb, generator, [
    #{
        shared_key => <<"global_key">>,
        conflict_ratio => 0,
        generator => {uniform_int, 1000000}
    }
]}}.
{value_generator, {fixed_bin, 256}}.

{pre_hook, {hook_grb, start}}.
{pre_hook_args, [[{conn_pool_size, 6},
                  {conection_opts, #{id_len => 16}},
                  {connection_port, 7878}]]}.

{driver,grb_bench_v2}.
{mode,max}.
{report_interval,5}.
