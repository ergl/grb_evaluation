{duration,5}.
{concurrent,75}.

{operations, [
   %% 20 red, 15% updates
   {readonly_blue, 68},
   {writeonly_blue, 12},

   {readonly_red, 17},
   {writeonly_red, 3}
]}.

{readonly_ops, 1}.
{writeonly_ops, 1}.
{mixed_read_write, {1, 1}}.

{crdt_type, grb_lww}.
{reuse_cvc, true}.
{retry_aborts, false}.

{strong_key_generator,
   {function, hook_grb, contention_generator, [
      #{
         contention_ratio => 1,
         ring_size => 32,
         default_generator => {uniform_int, 1000000}
      }
   ]}
}.

{key_generator, {uniform_int, 1000000}}.
{value_generator, {fixed_bin, 256}}.

{pre_hook, {hook_grb, start}}.
{pre_hook_args, [[{conn_pool_size, 6},
                  {conection_opts, #{id_len => 16}},
                  {connection_port, 7878}]]}.

{driver,grb_bench_v2}.
{mode,max}.
{report_interval,5}.
